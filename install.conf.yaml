- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore
    ~/.asdfrc: asdfrc

- shell:
    - [mkdir -p ~/.cache/zsh/, Create Cache directory for ZSH]
